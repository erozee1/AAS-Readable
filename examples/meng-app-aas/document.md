# Example Document View

This example directory was generated with an earlier `AAS-Readable` release.

The current package writes `document.*` as the top-level artifact instead of
`llm-context.*`.

For historical reference, the older compact artifact content is still available
in `llm-context.md`.
